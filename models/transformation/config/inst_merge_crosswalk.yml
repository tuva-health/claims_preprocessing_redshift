version: 2

models:
    - name: inst_merge_crosswalk
      description: Crosswalk of claim ids to a master grouper (encounter id)
      config:
        schema: "{{ var('data_set')}}_claims_preprocessing"
      columns:
        - name: merge_claim_id
          tests:
            - unique
            - not_null